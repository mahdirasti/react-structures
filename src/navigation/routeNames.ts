/**
 * Place all route names here
 * @example ```js
 * {
 *     "assets": "/assets",
 *     "assetDetails": "/assets/:id"
 * }
 * ```
 */

const routes = {
  login: "/login",
};

export default routes;
